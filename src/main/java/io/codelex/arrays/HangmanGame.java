/**//*
package io.codelex.arrays;
import java.util.Random;
public class HangmanGame {

    private static  String[] words = { "apple", "dog", "smile",
            "magic", "party", "summer", "vulcano", "instagram", "mountain" };

    public  static void main(String[] args) {
        String[] words = { "apple", "dog", "smile",
                "magic", "party", "summer", "vulcano", "instagram", "mountain" };


        String wordsFromArray = getRandomWord(words);
        System.out.println(wordsFromArray);



    }



        Random random = new Random();

        int randomWordIndex = random.nextInt(0,9);
        String randomWord = "";

        for (int i = 0;i < word.length; i++ ){
            if (i == randomWordIndex){
               randomWord = words[i];
            }

        }

        return randomWord;
    }

*/

